# Local-only material: talk decks, deck build output, planning notes.
# Nothing in here ships with the demo.
_local/

# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / web
node_modules/
dist/
*.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/
*.log
*.swp

# Runtime mock state (re-seeded from mocks/seeds/ on first run, or via
# the /api/reset endpoint on either service). Scoped per-agent under
# mocks/data/<agent_id>/ — first-cut and engineered each have their own files and never
# share runtime state. See mocks/client.py.
mocks/data/

# Engineered-agent episodic memory (one .md per customer; Bob's seed file is committed).
cs_agent_engineered/memory/episodic/customer_*.md
!cs_agent_engineered/memory/episodic/customer_cust_002.md

# Engineered-agent conversation memory (Strands FileSessionManager — per-customer history).
cs_agent_engineered/memory/sessions/

.deck-*
output/
